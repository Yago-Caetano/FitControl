package br.com.fitcontrol.fitcontrol.models;

import br.com.fitcontrol.fitcontrol.listenners.AccessCheckInListenner;
import org.junit.jupiter.api.Test;

import static br.com.fitcontrol.fitcontrol.events.EnumEventTypes.EVENT_TYPE_ACESS_CHECK_IN;
import static org.junit.jupiter.api.Assertions.assertEquals;

public class EventModelTest {
    /**
     *  This is a dummy example of how use junit API
     */
    @Test
    public void test()
    {
       // EventModel evento = new EventModel(,EVENT_TYPE_ACESS_CHECK_IN );

        //chack if the name is Catraca1
        //assertEquals(catracaModel.getModelo(),"Catraca1");

        //check if the id is 1
        //assertEquals(catracaModel.getId(),1);
    }
}
